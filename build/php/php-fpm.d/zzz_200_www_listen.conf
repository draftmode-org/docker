[www]
; mount path for socker - defined in deploymentment or docker run
; fastcgi_pass directive has to have access (share(d) between php and webserver)
listen = ${PHP_FPM_LISTEN}
listen.group = ${PHP_FPM_LISTEN_GROUP}
listen.mode = 0660
